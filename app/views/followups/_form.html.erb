<div class="formdiv">

 <%= form_for([job, followup]) do |form| %>
  <% if followup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(followup.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% followup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field marginal">
  <%= form.label :number %>
  <%= form.select :number, ['1st', '2nd','3rd','4th','5th'], class:"custom-select marginal"%>
  </div>

  <div class="field">
    <%= form.label :mode %>
    <%= form.select :mode, ['Email', 'Phone', 'LinkedIn', 'In Person', 'Other'],class:"custom-select"  %>
  </div>

  <div class="field">
    <%= form.label :contact %>
    <%= form.text_field :contact, autocomplete: 'off', class:"form-control"  %>
  </div>

  
    

  

  <div class="actions marginal">
    <%= form.submit class:"btn btn-info", id:"white" %>
  </div>
</div>
<% end %>
